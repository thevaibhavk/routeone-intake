NEXT_PUBLIC_APP_URL=http://localhost:3000
APP_SECRET=replace-with-a-long-random-secret
ADMIN_PASSWORD=change-this-admin-password

# Upstash Redis (REQUIRED) — create a free DB at https://console.upstash.com
# and copy these from the database's REST API section.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

MAIL_FROM="Route One <no-reply@yourdomain.com>"
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
